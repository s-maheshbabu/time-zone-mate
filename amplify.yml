version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npx grunt build
        - npx grunt prepare_deploy
  artifacts:
    baseDirectory: deploy/static
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
